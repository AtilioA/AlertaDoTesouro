# Treasury bonds database
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=alertadotesouro
DB_PASSWORD=your_postgres_password
DB_DATABASE=alertadotesouro

# JWT secret for password hashing
JWT_SECRET=secret_passphrase

# Mailing credentials
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=25
MAIL_USER=your@mail.com
MAIL_PASSWORD=your_mail_password
MAIL_SECURE=false

# Redis config
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

NODE_ENV=development node --tls-min-v1.0 ts-node/register src/server.ts
